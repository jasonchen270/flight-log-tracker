# Local Postgres
DATABASE_URL=postgres://postgres:postgres@localhost:5432/flight_log

# Render adds ?sslmode=require automatically; the migrate script
# detects render.com hosts and turns on ssl with rejectUnauthorized:false.

PORT=4000
CLIENT_ORIGIN=http://localhost:5173
